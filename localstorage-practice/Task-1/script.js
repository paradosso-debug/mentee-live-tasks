// Ensures code runs only after the DOM has fully loaded
document.addEventListener("DOMContentLoaded", () => {
  // Select the input field, save button, and display area
  // Event listener for when the "Save" button is clicked
  // Display the saved name automatically when the page loads
});
