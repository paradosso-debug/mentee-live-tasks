//  STEP 1: Select HTML elements
const countryInput = document.getElementById("country-input");
const fetchButton = document.getElementById("fetch-country");
const countryInfo = document.getElementById("country-info");

// STEP 2: Create an asynchronous function to fetch country data

// Get the user's input value and remove extra spaces

//  STEP 3: Check if the input is empty
// If no country name is entered, display a message and stop the function.

//  STEP 4: Attempt to fetch data from the API

// Fetch data from the REST Countries API

//  STEP 5: Handle unsuccessful responses
// `.ok` checks if the response status is successful

//  STEP 6: Convert response to JSON format

// Extract the first country object from the response array

//  STEP 7: Display country information on the page

// STEP 8: Handle errors

// STEP 9: Add an event listener to the button
